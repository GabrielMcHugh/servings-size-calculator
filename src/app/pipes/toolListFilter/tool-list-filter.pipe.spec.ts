import { ToolListFilterPipe } from './tool-list-filter.pipe';

describe('ToolListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
